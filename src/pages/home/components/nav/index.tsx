import React, { FC } from 'react';

import styles from './index.less';

const Nav: FC = () => {
  return (
    <nav className={styles.wrapper}>
      <div>123</div>
    </nav>
  );
};

export default Nav;
